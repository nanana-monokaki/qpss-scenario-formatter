# 情報セキュリティについて

## 安心して利用できる点

- **アップロードファイルは保存されない**: セッション中のみメモリ上で処理され、終了後に自動削除されます
- **外部APIへの転送なし**: ファイルの変換処理はサーバー内で完結し、外部サービスへの転送は一切行いません
- **アカウント認証不要**: Googleアカウント等への接続・認証は不要です
- **HTTPS暗号化通信**: ブラウザとサーバー間の通信はSSL/TLSで暗号化されています
- **ソースコード公開**: 不審な処理がないことを誰でも検証できます

## 注意が必要な点

| リスク | 詳細 | 対策 |
|--------|------|------|
| アプリURLが公開状態 | URLを知っていれば誰でもアクセス可能 | URLの社外共有を避ける |
| 脚本内容がインターネットを経由 | アップロード時にクラウドサーバー（米国）を経由 | 機密性の高い脚本はローカル版（start.bat）を使用 |
| プリセットYAMLがGitHubで公開 | 案件固有の情報をプリセットに書くと外部から見える | プリセットに案件名・クライアント名を含めない |
| Streamlit Cloudの利用規約 | Snowflake社のサービスであり利用規約に準拠 | 社内のクラウドサービス利用ポリシーを確認 |

## 推奨運用ルール

1. **機密性の高い脚本** → ローカル版（start.bat）を使用（インターネット不使用）
2. **一般的な脚本** → Web版でOK
3. **プリセットYAML** → 案件名・クライアント名を含めない
4. **アプリURL** → 社内のみで共有し、外部に公開しない
